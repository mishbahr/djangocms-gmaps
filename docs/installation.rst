============
Installation
============

At the command line::

    $ easy_install djangocms-gmaps

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv djangocms-gmaps
    $ pip install djangocms-gmaps